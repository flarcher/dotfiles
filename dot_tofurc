
plugin_cache_dir = "$HOME/.terraform.d/plugin-cache"

provider_installation {
  filesystem_mirror {
    path    = "/home/fabrice/.terraform.d/providers"
    include = [ "*/*/*" ]
  }
  direct {
  }
}

